import nosotros from "../../data/nosotros.json";

export default function Nosotros() {
  return (
    <section id="nosotros" className="relative bg-[#070d1a] py-24 text-white">
      <div className="mx-auto grid max-w-7xl grid-cols-1 items-center gap-16 px-6 lg:grid-cols-2 lg:px-8">
        
        {/* FOTO / TARJETA PROFESIONAL */}
        <div className="relative">
          <div className="relative overflow-hidden rounded-3xl border border-slate-700/80 bg-gradient-to-br from-violet-950/70 to-slate-950 shadow-2xl">
            
            {/* Imagen real desde Decap CMS */}
            <div className="flex min-h-[520px] items-center justify-center overflow-hidden">
              {nosotros.foto ? (
                <img
                  src={nosotros.foto}
                  alt={nosotros.nombre_completo || "Foto profesional"}
                  className="h-full min-h-[520px] w-full object-cover object-center"
                />
              ) : (
                <div className="flex h-[520px] w-full items-center justify-center text-slate-400">
                  Sin foto profesional
                </div>
              )}
            </div>

            {/* Tarjeta inferior */}
            <div className="absolute bottom-6 left-6 right-6 rounded-2xl border border-slate-700 bg-slate-950/90 p-5 shadow-xl backdrop-blur-md">
              <h3 className="text-xl font-bold text-white">
                {nosotros.nombre_completo}
              </h3>

              <p className="mt-1 text-sm text-slate-400">
                {nosotros.cargo}
              </p>

              <div className="mt-4 flex flex-wrap gap-2">
                {nosotros.credencial1 && (
                  <span className="rounded-md border border-violet-500/40 bg-violet-500/10 px-3 py-1 text-xs font-medium text-violet-300">
                    {nosotros.credencial1}
                  </span>
                )}

                {nosotros.credencial2 && (
                  <span className="rounded-md border border-violet-500/40 bg-violet-500/10 px-3 py-1 text-xs font-medium text-violet-300">
                    {nosotros.credencial2}
                  </span>
                )}
              </div>
            </div>
          </div>
        </div>

        {/* TEXTO NOSOTROS */}
        <div>
          <div className="mb-6 inline-flex rounded-full border border-blue-500/40 bg-blue-500/10 px-5 py-2 text-xs font-bold uppercase tracking-widest text-blue-300">
            // Sobre Emunah
          </div>

          <h2 className="max-w-3xl text-4xl font-extrabold leading-tight tracking-tight text-white md:text-5xl">
            {nosotros.titulo}
          </h2>

          <div className="mt-8 space-y-6 text-lg leading-8 text-slate-400">
            {nosotros.parrafo1 && <p>{nosotros.parrafo1}</p>}
            {nosotros.parrafo2 && <p>{nosotros.parrafo2}</p>}
          </div>

          {nosotros.cita && (
            <blockquote className="mt-8 border-l-4 border-violet-500 bg-slate-900/60 px-6 py-5 text-lg font-semibold italic leading-8 text-slate-100">
              {nosotros.cita}
            </blockquote>
          )}

          <div className="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2">
            <Feature text="Cumplimiento normativo del RNE" />
            <Feature text="Eficiencia estructural garantizada" />
            <Feature text="Obras públicas y privadas" />
            <Feature text="Capacitación con casos reales" />
          </div>
        </div>
      </div>
    </section>
  );
}

function Feature({ text }) {
  return (
    <div className="flex items-center gap-4">
      <span className="flex h-7 w-7 items-center justify-center rounded-lg bg-gradient-to-br from-blue-500 to-violet-600 text-sm font-bold text-white shadow-lg">
        ✓
      </span>
      <span className="font-semibold text-slate-200">{text}</span>
    </div>
  );
}
